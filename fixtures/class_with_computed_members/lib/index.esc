val bar = "bar"
val baz = "baz"
class Foo {
    [bar]: number,
    [baz](self) {
        return self[bar]
    },
    constructor(mut self, barVal: number = 42) {
        self[bar] = barVal
    }
}

val foo = Foo()
val fooBar = foo[bar]
val fooBaz = foo[baz]()
