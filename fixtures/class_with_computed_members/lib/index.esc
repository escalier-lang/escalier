val bar = "bar"
val baz = "baz"
class Foo {
    [bar]: number,
    [baz](self) {
        return self[bar]
    },
    constructor(mut self, barVal?: number) {
        self[bar] = if barVal != undefined { barVal } else { 42 }
    }
}

val foo = Foo()
val fooBar = foo[bar]
val fooBaz = foo[baz]()
