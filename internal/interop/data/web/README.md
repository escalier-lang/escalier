# `web:*` pseudo-packages

Browser / DOM APIs. The resolver maps `web:foo` to `web/foo.esc` in
this directory. Multi-word packages use underscores
(`web:web_rtc` → `web/web_rtc.esc`).

See `planning/builtins/implementation_plan.md` §2.2.
