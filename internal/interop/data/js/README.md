# `js:*` pseudo-packages

ECMAScript / language-level builtins. The resolver maps `js:foo` to
`js/foo.esc` in this directory. Multi-word packages use underscores
(`js:typed_arrays` → `js/typed_arrays.esc`).

See `planning/builtins/implementation_plan.md` §2.2.
