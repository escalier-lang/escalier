// Code generated by `dts_to_esc generate`. DO NOT EDIT.
// Its facts come from the pinned lib.*.d.ts set, the ECMA-262
// derived facts, and the overlay. Change one of those and re-run.

/**
 * Returns the number of leading zero bits in the 32-bit binary representation of a number.
 * @param x A numeric expression.
 */
@js("Math.clz32")
export declare fn clz32(x: number) -> number

/**
 * Returns the result of 32-bit multiplication of two numbers.
 * @param x First number
 * @param y Second number
 */
@js("Math.imul")
export declare fn imul(x: number, y: number) -> number

/**
 * Returns the sign of the x, indicating whether x is positive, negative or zero.
 * @param x The numeric expression to test
 */
@js("Math.sign")
export declare fn sign(x: number) -> number

/**
 * Returns the base 10 logarithm of a number.
 * @param x A numeric expression.
 */
@js("Math.log10")
export declare fn log10(x: number) -> number

/**
 * Returns the base 2 logarithm of a number.
 * @param x A numeric expression.
 */
@js("Math.log2")
export declare fn log2(x: number) -> number

/**
 * Returns the natural logarithm of 1 + x.
 * @param x A numeric expression.
 */
@js("Math.log1p")
export declare fn log1p(x: number) -> number

/**
 * Returns the result of (e^x - 1), which is an implementation-dependent approximation to
 * subtracting 1 from the exponential function of x (e raised to the power of x, where e
 * is the base of the natural logarithms).
 * @param x A numeric expression.
 */
@js("Math.expm1")
export declare fn expm1(x: number) -> number

/**
 * Returns the hyperbolic cosine of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.cosh")
export declare fn cosh(x: number) -> number

/**
 * Returns the hyperbolic sine of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.sinh")
export declare fn sinh(x: number) -> number

/**
 * Returns the hyperbolic tangent of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.tanh")
export declare fn tanh(x: number) -> number

/**
 * Returns the inverse hyperbolic cosine of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.acosh")
export declare fn acosh(x: number) -> number

/**
 * Returns the inverse hyperbolic sine of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.asinh")
export declare fn asinh(x: number) -> number

/**
 * Returns the inverse hyperbolic tangent of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.atanh")
export declare fn atanh(x: number) -> number

/**
 * Returns the square root of the sum of squares of its arguments.
 * @param values Values to compute the square root for.
 *     If no arguments are passed, the result is +0.
 *     If there is only one argument, the result is the absolute value.
 *     If any argument is +Infinity or -Infinity, the result is +Infinity.
 *     If any argument is NaN, the result is NaN.
 *     If all arguments are either +0 or −0, the result is +0.
 */
@js("Math.hypot")
export declare fn hypot(...values: Array<number>) -> number

/**
 * Returns the integral part of the a numeric expression, x, removing any fractional digits.
 * If x is already an integer, the result is x.
 * @param x A numeric expression.
 */
@js("Math.trunc")
export declare fn trunc(x: number) -> number

/**
 * Returns the nearest single precision float representation of a number.
 * @param x A numeric expression.
 */
@js("Math.fround")
export declare fn fround(x: number) -> number

/**
 * Returns an implementation-dependent approximation to the cube root of number.
 * @param x A numeric expression.
 */
@js("Math.cbrt")
export declare fn cbrt(x: number) -> number

/** The mathematical constant e. This is Euler's number, the base of natural logarithms. */
@js("Math.E")
export declare val E: number

/** The natural logarithm of 10. */
@js("Math.LN10")
export declare val LN10: number

/** The natural logarithm of 2. */
@js("Math.LN2")
export declare val LN2: number

/** The base-2 logarithm of e. */
@js("Math.LOG2E")
export declare val LOG2E: number

/** The base-10 logarithm of e. */
@js("Math.LOG10E")
export declare val LOG10E: number

/** Pi. This is the ratio of the circumference of a circle to its diameter. */
@js("Math.PI")
export declare val PI: number

/** The square root of 0.5, or, equivalently, one divided by the square root of 2. */
@js("Math.SQRT1_2")
export declare val SQRT1_2: number

/** The square root of 2. */
@js("Math.SQRT2")
export declare val SQRT2: number

/**
 * Returns the absolute value of a number (the value without regard to whether it is positive or negative).
 * For example, the absolute value of -5 is the same as the absolute value of 5.
 * @param x A numeric expression for which the absolute value is needed.
 */
@js("Math.abs")
export declare fn abs(x: number) -> number

/**
 * Returns the arc cosine (or inverse cosine) of a number.
 * @param x A numeric expression.
 */
@js("Math.acos")
export declare fn acos(x: number) -> number

/**
 * Returns the arcsine of a number.
 * @param x A numeric expression.
 */
@js("Math.asin")
export declare fn asin(x: number) -> number

/**
 * Returns the arctangent of a number.
 * @param x A numeric expression for which the arctangent is needed.
 */
@js("Math.atan")
export declare fn atan(x: number) -> number

/**
 * Returns the angle (in radians) between the X axis and the line going through both the origin and the given point.
 * @param y A numeric expression representing the cartesian y-coordinate.
 * @param x A numeric expression representing the cartesian x-coordinate.
 */
@js("Math.atan2")
export declare fn atan2(y: number, x: number) -> number

/**
 * Returns the smallest integer greater than or equal to its numeric argument.
 * @param x A numeric expression.
 */
@js("Math.ceil")
export declare fn ceil(x: number) -> number

/**
 * Returns the cosine of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.cos")
export declare fn cos(x: number) -> number

/**
 * Returns e (the base of natural logarithms) raised to a power.
 * @param x A numeric expression representing the power of e.
 */
@js("Math.exp")
export declare fn exp(x: number) -> number

/**
 * Returns the greatest integer less than or equal to its numeric argument.
 * @param x A numeric expression.
 */
@js("Math.floor")
export declare fn floor(x: number) -> number

/**
 * Returns the natural logarithm (base e) of a number.
 * @param x A numeric expression.
 */
@js("Math.log")
export declare fn log(x: number) -> number

/**
 * Returns the larger of a set of supplied numeric expressions.
 * @param values Numeric expressions to be evaluated.
 */
@js("Math.max")
export declare fn max(...values: Array<number>) -> number

/**
 * Returns the smaller of a set of supplied numeric expressions.
 * @param values Numeric expressions to be evaluated.
 */
@js("Math.min")
export declare fn min(...values: Array<number>) -> number

/**
 * Returns the value of a base expression taken to a specified power.
 * @param x The base value of the expression.
 * @param y The exponent value of the expression.
 */
@js("Math.pow")
export declare fn pow(x: number, y: number) -> number

/** Returns a pseudorandom number between 0 and 1. */
@js("Math.random")
export declare fn random() -> number

/**
 * Returns a supplied numeric expression rounded to the nearest integer.
 * @param x The value to be rounded to the nearest integer.
 */
@js("Math.round")
export declare fn round(x: number) -> number

/**
 * Returns the sine of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.sin")
export declare fn sin(x: number) -> number

/**
 * Returns the square root of a number.
 * @param x A numeric expression.
 */
@js("Math.sqrt")
export declare fn sqrt(x: number) -> number

/**
 * Returns the tangent of a number.
 * @param x A numeric expression that contains an angle measured in radians.
 */
@js("Math.tan")
export declare fn tan(x: number) -> number

/**
 * Returns the nearest half precision float representation of a number.
 * @param x A numeric expression.
 */
@js("Math.f16round")
export declare fn f16round(x: number) -> number
