// Formats the serializer sources with the same settings the vendored ESMeta
// tree uses, so the Scala here reads like the Scala it is compiled against.
addSbtPlugin("org.scalameta" % "sbt-scalafmt" % "2.5.0")
